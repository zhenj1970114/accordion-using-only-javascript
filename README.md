# Accordion Using Only JavaScript

You’ll need to first compile the CSS using Sass before viewing this demo.

`sass --unix-newlines --sourcemap=none --style expanded --watch sass/style.scss:css/style.css`
